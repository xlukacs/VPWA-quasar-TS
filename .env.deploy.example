# Docker image
DOCKER_USERNAME=your_dockerhub_username

# App
APP_KEY=nlnpGYSTleLKKrMtkZSPJfI8tHJWMIa9

# Postgres (managed by Dokploy as a separate service)
PG_HOST=your_dokploy_postgres_host
PG_PORT=5432
PG_USER=vpwa_user
PG_PASSWORD=vpwa_pass
PG_DB_NAME=vpwa

# Cloudflare Tunnel
# Create a tunnel at https://one.dash.cloudflare.com -> Networks -> Tunnels
# Point the tunnel's public hostname to http://slack_clone:80
CLOUDFLARE_TUNNEL_TOKEN=your_tunnel_token_here

# Portfolio lock - blocks all API access (login, register, messaging)
# Set to "false" to unlock the app for actual use
DEMO_LOCKED=true
